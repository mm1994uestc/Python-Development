#!/usr/bin/python
# -*- coding: UTF-8 -*-
def fibonacci_func(Num):
   count=0;
   #FIFO = [0,1]; 
   Total = 0;
   print("The grade of fibonacci is ",Num);
   #for count in range(L):
   #Total = FIFO[0]+FIFO[1];
   #FIFO[0] = FIFO[1];
   #FIFO[1] = Total;
   print(Total,count);
   count+=1;
   return;